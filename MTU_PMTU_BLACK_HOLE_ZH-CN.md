# MTU/PMTU 黑洞导致的移动网络图片上传失败

适用于 Suntek HC-940Ultra、HC-950Ultra 和 HC-960Ultra 相机系列的技术文档。

[English](MTU_PMTU_BLACK_HOLE_EN.md) |
[简体中文](MTU_PMTU_BLACK_HOLE_ZH-CN.md)

文档日期：2026-07-29

## 1. 摘要

HC-950Ultra 使用 Simbase 物联网 SIM 卡时可以成功建立移动数据连接，但图片上传会一直停留在
`0%`。云端配置和 P2P 实时视频仍能正常工作。受控 A/B 测试证明：只将 CPU2 Linux
移动网络接口的 MTU 从 1500 改为 1200，不改变任何其他参数，同一图片即可立即上传成功。

该现象符合路径 MTU 发现（PMTUD）黑洞的典型特征：

1. 相机按照接口 MTU 1500 生成 TCP 分段。
2. 物联网核心网、漫游链路、隧道或互联网出口中的实际端到端 MTU 更小。
3. 过大的数据包无法通过完整链路。
4. 所需的 ICMP 反馈被过滤、丢失，或者没有被正确处理。
5. TCP 不断重传仍然无法通过的数据。
6. HTTPS 图片上传一直等待内部超时，而屏幕始终显示 `0%`。

建议的现场长期解决方案是采用事件驱动、持久生效的 MTU 1300。该数值已在
HC-950Ultra 上确认：冷启动后仍然保持，并且在两个已测试的相机工作模式中均可正常使用。
MTU 1200 仍是目前最保守的已知可用值。

## 2. 适用范围和验证状态

| 型号和固件日期 | 技术兼容性 | 实机验证 |
|---|---|---|
| HC-950Ultra，2026-05-27 | 已确认 | MTU 1300 重启后保持有效，并在两个已测试工作模式中正常工作 |
| HC-940Ultra，2025-04-23 | 已通过固件检查和安装模拟确认 | 待实机验证 |
| HC-960Ultra，2026-03-26 | 已通过固件检查和安装模拟确认 | 待实机验证 |

检查结果表明，这三个固件版本包含完全相同的四个相关网络脚本，并且同时具有旧版网络路径和当前
RIL 网络路径。但是，由于不同版本的主网络程序并不相同，每个型号和固件版本仍必须使用各自专用的
补丁包。

静态兼容性不等于实机批准。HC-940Ultra 和 HC-960Ultra 仍需在真实相机上完成冷启动、
重新连接、DHCP 更新和图片上传测试。

## 3. 已确认的故障表现

问题在以下网络配置中复现：

- 物联网服务商：Simbase
- IMSI 前缀：`20404`
- APN：`simbase`
- APN 用户名和密码：空
- 数据漫游：已启用
- 移动网络接口：通常为 `eth0`
- 默认接口 MTU：1500

实际现象：

- 移动数据注册成功。
- 云端配置获取成功。
- 云端控制通道正常。
- P2P 实时视频正常，约为 90 kbit/s。
- 通过 HTTPS 上传 JPEG 时一直停留在 `0%`。
- 同一相机更换普通消费级 SIM 后，MTU 1500 可以正常上传。
- 物联网 SIM 分别使用 Telekom 和 Vodafone 作为漫游接入网络时，均观察到 MTU 1500
  上传失败。

MTU 1350 的成功测试是在物联网 SIM 注册到 Telekom 网络时完成的。由于相机不支持手动选择网络，
因此尚未在 Vodafone 漫游链路上单独验证 MTU 1350。

## 4. 受控 A/B 测试结果

| 测试条件 | 结果 |
|---|---|
| Simbase 物联网 SIM，接口 MTU 1500 | 图片上传一直停留在 `0%` |
| 相机、SIM、数据会话、APN 和云端账号不变；仅将 MTU 改为 1200 | 图片上传成功 |
| Simbase 物联网 SIM，MTU 1350，Telekom 漫游链路 | 图片上传成功 |
| HC-950Ultra 持久设置 MTU 1300 | 重启后仍然有效，并在两个已测试工作模式中正常工作 |
| 普通消费级 SIM，MTU 1500 | 图片在数秒内上传成功 |
| 使用 Simbase 获取云端配置 | 正常 |
| 使用 Simbase 进行 P2P 实时视频 | 正常 |

在失败的 MTU 1500 测试和成功的 MTU 1200 测试之间，唯一改变的是接口 MTU。SIM、APN、
认证参数、无线连接、云端账号和上传操作均未改变。

## 5. 网络状态和诊断命令

原始 CPU2 Linux 状态显示：`eth0` 已启用，地址位于 `192.168.255.0/24`，
默认网关为 `192.168.255.1`，MTU 为 1500。

检查当前接口 MTU：

```sh
cat /sys/class/net/eth0/mtu
```

检查当前 DHCP 客户端是否使用 RIL 路径：

```sh
ps w | grep '[u]dhcpc'
```

检查 Linux 内核 PMTUD 模式：

```sh
cat /proc/sys/net/ipv4/ip_no_pmtu_disc
```

受控测试中的值为：

```text
ip_no_pmtu_disc = 0
```

该值表示正常启用 PMTUD。即使 PMTUD 已启用，仍然必须降低接口 MTU，这进一步支持以下诊断：
有效的 PMTU 反馈没有到达相机，或者没有成功修正受影响的 TCP 数据流。

临时诊断方法：

```sh
ifconfig eth0 mtu 1200
cat /sys/class/net/eth0/mtu
```

该手动命令不会持久保存。接口重新创建、模组重连、DHCP 事件、USB 网络复位或相机重启后，
MTU 都可能恢复为 1500。

## 6. 为什么只有图片上传失败

固件检查表明，相机的不同功能采用不同通信路径：

- 云端配置和控制主要交换较小的消息。
- P2P 实时视频采用独立的 P2P/UDP 通信路径和分包方式。
- 图片上传首先申请带签名的上传地址，然后通过持续的 HTTPS `PUT` 传输发送 JPEG。

HTTPS 上传更容易产生接近最大尺寸的 TCP 分段，因此更容易遇到 PMTU 黑洞。云端控制消息和
P2P 视频能够正常工作，并不能证明 MTU 1500 对应的 TCP/TLS 上传链路也能正常传输。

仅减小应用层上传缓冲区并不是等效修复。TCP 分段大小由接口 MTU 和协商得到的 MSS 决定。
正确修复必须处理接口 MTU、TCP MSS、模组提供的 MTU，或者 TCP 黑洞恢复机制。

## 7. 持久补丁设计

补丁使用一个小型配置文件：

```text
/etc/mtu_patch.conf
```

示例：

```text
MTU_VALUE=1300
```

只允许使用经过测试的数值：

| MTU | 用途 |
|---:|---|
| 1200 | 最保守且已确认有效的解决方案 |
| 1300 | 建议的长期值，具有一定安全余量 |
| 1350 | 已在测试的 Telekom 链路上确认，但余量更小 |

补丁必须同时覆盖旧版 GSM 路径和当前使用的 RIL 路径：

```text
/usr/share/gsmscripts/vk_rndis_ecm.sh
/usr/share/gsmscripts/vk_udhcpc_script.sh
/usr/share/rilscripts/vk_rndis_ecm.sh
/usr/share/rilscripts/vk_udhcpc_script.sh
```

### 7.1 接口启动钩子

两个 ECM/RNDIS 脚本必须：

1. 读取并验证配置的 MTU。
2. 只处理预期的移动网络接口（`eth0` 或 `usb0`）。
3. 接口启用后立即设置 MTU。
4. 从 sysfs 读取实际值；如果不一致则返回失败。
5. 只有 MTU 正确后才启动 DHCP。

预期诊断输出：

```text
[VK NET] eth0 MTU=1300
```

### 7.2 DHCP 事件钩子

两个 DHCP 脚本必须在以下事件中重新设置并验证 MTU：

- `deconfig`
- `bound`
- `renew`

在 `bound` 和 `renew` 事件中，必须先正确设置 MTU，然后再通知 CPU1 移动网络已经就绪。
这样可以避免云端连接或图片上传在短暂的 MTU 1500 时间窗口内启动。

### 7.3 不使用看门狗

无需安装周期运行的守护进程或看门狗。事件驱动钩子可以覆盖接口创建和 DHCP 变化，
同时不会给电池供电的相机增加持续唤醒。

## 8. 通用补丁程序要求

固件专用修改应由一个通用但针对具体型号的补丁程序部署。其传输和启动机制不属于本文范围。

补丁程序应当：

1. 要求必要的系统权限。
2. 识别准确的相机型号和固件版本。
3. 使用已知的型号专用指纹验证主网络程序。
4. 只有当目标脚本与已知原版或已知补丁版完全一致时才接受。
5. 拒绝未知或被本地修改过的脚本。
6. 安装前验证 shell 语法。
7. 为四个原始脚本同时创建内部备份和可移动介质备份。
8. 以原子方式写入 MTU 配置。
9. 在目标目录中暂存替换文件，并以原子方式完成替换。
10. 验证安装后的字节内容和权限。
11. 如果移动网络接口已经存在，立即应用所选 MTU。
12. 生成明确的成功、失败和诊断日志。
13. 支持 `install`、`verify` 和 `restore` 三种模式。
14. 安装中断后，如果系统处于部分原版、部分补丁版的混合状态，能够安全继续。

每个受支持的型号和固件版本都必须拥有各自的白名单指纹和恢复数据。仅仅因为脚本相似，
不能对未知版本执行补丁。

## 9. 第一版补丁为什么失败

第一版只修改了：

```text
/usr/share/gsmscripts/
```

安装过程虽然完成，并且可以将当时已经存在的 `eth0` 改为 MTU 1300，但相机随后建立正常连接时
使用的是较新的 `[VK RIL]` 状态机：

```text
/usr/share/rilscripts/
```

该活动路径重新创建或配置接口时使用 MTU 1500。因此，安装程序报告成功并不能证明 MTU 会长期保持。

修正版同时修改全部四个脚本。活动 RIL 脚本在 DHCP 启动前设置 MTU，并在 DHCP 事件中再次强制设置；
GSM 脚本则作为旧版或备用路径继续受到保护。

## 10. 安装后验证

应用对应型号的补丁后：

1. 移除补丁介质。
2. 插入相机正常使用的存储卡。
3. 完整冷启动相机。
4. 等待移动数据连接建立。
5. 检查：

```sh
cat /etc/mtu_patch.conf
cat /sys/class/net/eth0/mtu
ps w | grep '[u]dhcpc'
```

建议配置的预期输出：

```text
MTU_VALUE=1300
1300
```

还应确认 DHCP 命令指向当前使用的 RIL DHCP 脚本。

随后完成以下测试：

- 至少连续上传 20 张原始分辨率图片；
- 再次冷启动后至少上传 20 张图片；
- 上传缩小分辨率图片；
- 触发一次模组或移动网络重新连接；
- 触发一次 DHCP 更新；
- 测试相机的两个工作模式；
- 获取云端配置；
- 测试 P2P 实时视频；
- 使用普通消费级 SIM 进行回归测试。

每次重启或重新连接后，都应再次检查接口 MTU。

## 11. 恢复行为

恢复模式应当：

1. 确认每个当前目标脚本都是已知原版或已知补丁版。
2. 以原子方式恢复全部四个原始脚本。
3. 删除持久 MTU 配置。
4. 将当前已经存在的移动接口恢复为 MTU 1500。
5. 验证恢复后的字节内容和当前 MTU。
6. 保留外部备份，以便手动恢复。

后续完整的厂家固件更新也可能替换 CPU2 根文件系统并移除补丁。因此，每次固件升级后都必须重新检查 MTU。

## 12. 建议的厂家正式修复

官方固件应在通用网络代码中解决问题，而不是依赖现场补丁。建议：

- 如果模组或 PDP 上下文提供网络 MTU，则读取并应用该值；
- 支持针对不同运营商配置 APN MTU；
- 使用 1300 或 1200 等安全默认值；
- 每次接口重新创建、网络重连和 DHCP 事件后重新应用；
- 在建立云端连接之前完成设置；
- 验证并记录活动接口及其 MTU；
- 如果内核支持，可启用 TCP MTU probing；
- HTTPS 上传超时后，以更低的有效 MTU/MSS 自动重试；
- 保留待上传图片，并在网络恢复后重试；
- 分别报告 DNS、TCP、TLS、HTTP 和上传阶段错误。

## 13. 限制和隐私

- 只有列出的 HC-950Ultra 固件已经完成实机运行验证。
- HC-940Ultra 和 HC-960Ultra 仍需实机验证。
- MTU 1350 只在已测试的 Telekom 漫游链路上得到确认。
- 未来固件可能使用不同脚本或不同事件顺序。
- 相机、UART 和模组日志可能包含 IMEI、IMSI、ICCID、MAC 地址、访问令牌和带签名的上传地址。
  公开发布前必须删除或隐藏这些信息。
